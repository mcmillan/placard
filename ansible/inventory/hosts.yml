---
placard:
  hosts:
    placard-01:
      ansible_host: 192.168.10.50
      hdmi_rate: 50
      # placard_release: "e0c6a7b"   # pin a short-sha tag; default tracks latest
  vars:
    ansible_user: root
